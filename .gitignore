node_modules
.next
out
.env
.env.local
.DS_Store
*.log

# Playwright
/test-results
/playwright-report
tsconfig.tsbuildinfo

# Next.js generated agent rules
AGENTS.md
CLAUDE.md

# Regenerated by every next dev / next build, with different contents each
# way, so tracking it guarantees a permanently dirty tree.
next-env.d.ts
